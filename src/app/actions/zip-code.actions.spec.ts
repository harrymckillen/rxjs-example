import * as fromZipCode from './zip-code.actions';

xdescribe('AddZipCode', () => {
  it('should return an action', () => {
    // expect(fromZipCode.AddZipCode().type).toBe('[ZipCode] Add ZipCode');
  });
});

xdescribe('RemoveZipCode', () => {
  it('should return an action', () => {
    // expect(fromZipCode.RemoveZipCode().type).toBe('[ZipCode] Remove ZipCode');
  });
});
